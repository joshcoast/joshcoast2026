{
  "name": "coast-blocks",
  "version": "1.0.0",
  "description": "Custom blocks with shared spacing controls (All screens, Medium, Small).",
  "scripts": {
    "build": "wp-scripts build src/index.js --output-path=build",
    "start": "wp-scripts start src/index.js --output-path=build"
  },
  "devDependencies": {
    "@wordpress/scripts": "^26.1.0"
  }
}
